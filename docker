# 1. 创建配置目录（如果不存在）
mkdir -p /etc/docker

# 2. 编辑配置文件
vi /etc/docker/daemon.json

{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://docker.1ms.run",
    "https://dockerproxy.com"
  ]
}
